[build]
  command = "npm run build"
  publish = "dist"

[context.production.environment]
# Add any site-specific environment variables here. The deploy workflow will
# use repository secrets NETLIFY_AUTH_TOKEN and NETLIFY_SITE_ID when available.
